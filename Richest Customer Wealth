class Solution:
    def maximumWealth(self, accounts: List[List[int]]) -> int:
     richest = 0

     for customer in accounts:
        total = 0

        for money in customer:
            total += money

            if total > richest:
                richest = total

     return richest    
